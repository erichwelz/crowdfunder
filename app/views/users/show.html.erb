<h1>Dashboard</h1>
<hr>
<div class="row">
	<div class="large-4 columns">
		<%= image_tag @user.avatar.url(:medium) %>
	</div>
	<div class="large-4 columns">
	 <h1 id="users-name"><%= @user.first_name %> <%= @user.last_name %></h1>
		<%= @user.email %> 
	<br>
		

	</div>

	<div class="large-4 columns">
		
	<%= link_to "create project", new_project_path, class: "button round" %>
	<%= link_to "Edit My Information", edit_user_path(@user), class: "button round" %>
	<div class="panel left">
	Total Contributions: <br>
	<h3>$<%= @user.user_total_contributions %></h3> 
	</div>

	</div>
</div>
<hr>
<div class="row testing">

		<div class="large-6 medium-6 small-12 columns hello">
			<h2>My Contributions</h2>
			
			<% x = 1 %>
			
			<table>	
			<thead> <td>&nbsp</td> <td>Project</td> <td>contribution</td> </thead>
			<% @user.contributions.each do |contribution| %>

				<% project = Project.where(:id => contribution.project_id) %>
					<% project.each do |proj| %>
					<tr>
					<td><%= x %></td>
					<td><%= proj.title %></td>
				<% end %>
					<td> $<%= contribution.amount_in_dollars %> </td> 
				</tr>
				<% x+=1 %>
			<% end %>

		

		</table>
		</div>
		
		<div class="large-6 medium-6 small-12 columns">
			<h2>My Projects</h2>
			
		Private Projects

			
		<% @user.projects.each do |project| %>
		
		<%= project.title %> 
		<%= project.percentage_of_goal %>%
		<%= link_to "P Page", project_path(project) %>
		<%= project.public_date %>
		<br> 


		Public Projects
		<% end %>


		</div>
</div>
<hr>





